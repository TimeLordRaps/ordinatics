name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      checks: read
      contents: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Require a protected-main commit and matching package version
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git fetch origin main
          git merge-base --is-ancestor "$GITHUB_SHA" origin/main
          test "$(git rev-parse "${GITHUB_REF}^{commit}")" = "$GITHUB_SHA"
          VERSION="${GITHUB_REF_NAME#v}"
          PACKAGE_VERSION="$(python -c 'import tomllib; print(tomllib.load(open("pyproject.toml", "rb"))["project"]["version"])')"
          test "$VERSION" = "$PACKAGE_VERSION"
          test "$(gh api "repos/$GITHUB_REPOSITORY/commits/$GITHUB_SHA/check-runs" --jq '[.check_runs[] | select(.name == "conformance-gate" and .conclusion == "success")] | length')" -ge 1

      - name: Re-run conformance on the tagged checkout
        run: |
          python -m pip install ".[test,release]"
          python -m pytest -q
          python -m compileall -q src scripts

      - name: Build exact source, reproducible wheel, and external manifest
        run: |
          python scripts/release_artifacts.py build \
            --ref "$GITHUB_REF" \
            --release "${GITHUB_REF_NAME#v}" \
            --output-dir dist

      - name: Smoke-test the exact wheel and release boundary
        run: |
          python -m twine check dist/*.whl dist/*.tar.gz
          python scripts/check_release_boundary.py dist/*.zip dist/*.whl dist/*.tar.gz
          python scripts/check_installed_wheel.py --wheel-dir dist

      - name: Attest every published artifact
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: |
            dist/*.zip
            dist/*.whl
            dist/*.tar.gz
            dist/*.manifest.json
            paper/*.pdf

      - name: Write bounded release notes
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          TAG_OBJECT="$(git rev-parse "$GITHUB_REF_NAME")"
          TAG_VERIFIED="$(gh api "repos/$GITHUB_REPOSITORY/git/tags/$TAG_OBJECT" --jq '.verification.verified')"
          TAG_REASON="$(gh api "repos/$GITHUB_REPOSITORY/git/tags/$TAG_OBJECT" --jq '.verification.reason')"
          if test "$TAG_VERIFIED" = true; then
            TAG_STATUS="SIGNED_AND_GITHUB_VERIFIED"
          else
            TAG_STATUS="NOT_GITHUB_VERIFIED"
          fi
          {
            printf 'Source tag signature status: **%s** (`%s`).\n\n' "$TAG_STATUS" "$TAG_REASON"
            echo "Every uploaded asset is bound to the exact public commit by an external release manifest and GitHub artifact attestation. The wheel and source distribution published to PyPI are the same tested files attached here. Verify a GitHub asset with:"
            echo
            echo '```bash'
            echo "gh attestation verify PATH_TO_ASSET --repo $GITHUB_REPOSITORY"
            echo '```'
            echo
            awk -v version="$VERSION" '
              $0 ~ "^## " version " " {printing=1; next}
              printing && /^## / {exit}
              printing {print}
            ' CHANGELOG.md
          } > release-notes.md

      - name: Publish only the tested and attested artifacts
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            dist/*.zip dist/*.whl dist/*.tar.gz dist/*.manifest.json paper/*.pdf \
            --repo "$GITHUB_REPOSITORY" \
            --verify-tag \
            --title "ordinatics ${GITHUB_REF_NAME}" \
            --notes-file release-notes.md

      - name: Stage only the Python distributions for PyPI
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ordinatics-${{ github.ref_name }}
          path: |
            dist/*.whl
            dist/*.tar.gz
          if-no-files-found: error
          retention-days: 7

  publish-pypi:
    needs: release
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/ordinatics
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download the exact tested Python distributions
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ordinatics-${{ github.ref_name }}
          path: dist

      - name: Publish through PyPI Trusted Publishing
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
        with:
          packages-dir: dist
          print-hash: true
